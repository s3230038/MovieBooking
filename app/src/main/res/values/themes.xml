<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MovieBookingApp" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Base.Theme.MovieBooking" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Set the status bar color to black -->
        <item name="android:statusBarColor">@android:color/black</item>

        <!-- Optional: Ensure light content on the status bar for better visibility -->
        <item name="android:windowLightStatusBar">false</item>
    </style>


</resources>